{
  "name": "divine-moderation-service",
  "version": "0.1.0",
  "description": "Content moderation worker for Divine video uploads",
  "license": "MPL-2.0",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail",
    "lint": "node scripts/lint.mjs",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "jose": "^6.1.3",
    "nostr-tools": "^2.7.2"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.5.0",
    "vitest": "^2.0.0",
    "wrangler": "^4.42.2"
  }
}
